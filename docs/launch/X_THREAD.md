# X / Twitter thread draft (do not post without confirmation)

1/ NeuroCode: see the architecture of a Python codebase as a hierarchical knowledge graph—on your machine.

Tree-sitter → Neo4j → React explorer.

2/ Built for the “I just joined a monorepo” moment. Expand packages → modules → classes → functions. No SaaS upload.

3/ Fastest path:

```bash
git clone https://github.com/mohitmishra786/neuro-code
cd neuro-code && make demo
```

4/ Early 0.1.x / MIT. Feedback on setup friction welcome.
